{
  "name": "@model-create/epanet-js-website",
  "version": "0.3.0",
  "scripts": {
    "start": "docker run --rm -it -p 8000:8000 -v \"${PWD}:/docs\" squidfunk/mkdocs-material",
    "deploy": "gh-pages -d site"
  },
  "devDependencies": {
    "gh-pages": "^2.2.0"
  }
}
